## OpenBB MCP Server for Smithery

This repo packages OpenBB for deployment on Smithery using the container runtime. It installs `openbb[all]` to enable all OpenBB data extensions and supports provider API keys via environment variables mapped into OpenBB's `~/.openbb_platform/user_settings.json` at container startup.
