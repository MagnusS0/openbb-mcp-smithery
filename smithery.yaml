startCommand:
    type: http
    configSchema:
      type: object
      required: ["apiKey"]
      properties:
        apiKey:
          type: string
          title: "Personal Access Token (PAT)"
          description: "Required OpenBB PAT used to authenticate requests"
